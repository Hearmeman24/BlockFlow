import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Generate',
  description: 'Build and run video and image generation pipelines.',
}

export default function GeneratePage() {
  return null
}
