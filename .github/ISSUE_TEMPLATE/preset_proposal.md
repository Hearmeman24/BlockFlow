---
name: Preset proposal
about: Propose a new preset for the BlockFlow preset registry
labels: preset
---

## Preset name + ID

- **Display name**:
- **id** (kebab-case): `e.g. wan22-i2v`

## Why this preset

<!-- What model / workflow does it expose? Why is it worth adding to the registry? -->

## Workflow JSON

<!-- Attach the ComfyUI workflow JSON (API format — File → Export → API Format in ComfyUI). Confirm it runs end-to-end before proposing. -->

## Model files

<!-- List the models the preset needs, with URLs + destination paths + SHA256 if known. -->

| Source | URL | Destination | SHA256 |
|---|---|---|---|
| | | | |

## Disk size estimate

<!-- Approximate GB the preset needs on the ComfyGen volume (sum of all model files + ~20% buffer). -->

## ComfyGen min version

<!-- Which ComfyGen worker version is needed? If you don't know, leave blank and we'll fill in during review. -->

## Tested against

<!-- e.g. "Tested on ComfyGen 0.2.0 + RTX 5090 on RunPod (budget tier). Submitted via wizard-provisioned endpoint." -->
